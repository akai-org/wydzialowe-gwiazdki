import React from 'react';

const Person = (props) =>{

    return(
        <div>
      {props.profession} {props.name}  {props.surname}    <br />
     

        </div>
    )
}

export default Person;